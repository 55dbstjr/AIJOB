# IW-228 — mu CI/CD 세팅

> 상위: IW-184
> 생성일: 2026-04-07
> 상태: 진행 중

## 목표
mu 프로젝트의 CI/CD 파이프라인 구축 및 설정 작업.

- **배포 플로우**: master 커밋 → deploy_mu.sh <mico|test> → 대상 브랜치(mico-prod/ingkle-dev)로 빌드 파일 동기화 → push → DevOps CI/CD가 Docker build → Harbor push → K8s 배포
- **브랜치 전략**: master(개발), mico-prod(프로덕션 192.168.3.80), ingkle-dev(테스트 nazare)
- **Docker 빌드**: Dockerfile(전체빌드), Dockerfile.cached(deps이미지 활용 빠른빌드), Dockerfile.deps(의존성전용), Dockerfile.prebuilt(바이너리기반 경량)
- **주요 의존성**: aws-sdk-cpp v1.11.379(S3/Ceph), paho-mqttpp3(MQTT), nlohmann-json, Vulkan
- **환경변수**: CEPH_HOST/PORT/ACCESS_KEY/SECRET_KEY, MQTT_HOST/PORT/USERNAME/PASSWORD — git 미포함, DevOps 별도 전달

## 작업 완료 조건
- 작업 완료
- 이 파일 삭제 + Jira IW-228 Done 전환
